```console
$ recon -d -u -q 'select abs_path,size from files order by abs_path ASC'
┌──────────────────[..]───────────────────────────────────────────────────┬──────┐
│ abs_path                                                                │ size │
├──────────────────[..]───────────────────────────────────────────────────┼──────┤
│ [CWD]/test.txt │ 12   │
└──────────────────[..]───────────────────────────────────────────────────┴──────┘
1 of 1 files in [..]ms

```
